spring:
  datasource:
    driver-class-name: postgres3
    url: localhost:5435
    username: postgres
    password: postgres
jpa:
  show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect